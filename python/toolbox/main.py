class toolbox:
	def __init__(self, tname):
	    self.tname = name # self is the instance, this is an argument that accepts the name of the job or what bash script to run
		self.uname = userName

	def fileUpload():
	    return os.system("sudo ssh -i ~/.ssh/llavePair.pem ubuntu@18.191.173.186")
